name: Start Pod

on:
  workflow_dispatch:

jobs:
  start:
    runs-on: ubuntu-latest
    steps:
      - name: Try cheapest pod first
        id: cheapest
        continue-on-error: true
        run: |
          echo "Trying CHEAPEST pod..."
          RESPONSE=$(curl -sS -X POST "https://api.runpod.io/graphql" \
            -H "Authorization: Bearer ${{ secrets.RUNPOD_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{\"query\":\"mutation { podResume(input: { podId: \\\"${{ secrets.RUNPOD_CHEAPEST_POD_ID }}\\\" }) { id desiredStatus } }\"}")

          echo "$RESPONSE"

          if echo "$RESPONSE" | grep -q '"errors"'; then
            echo "CHEAPEST pod unavailable."
            exit 1
          fi

          echo "CHEAPEST pod start request succeeded."

      - name: Try cheap pod if cheapest failed
        id: cheap
        if: steps.cheapest.outcome == 'failure'
        continue-on-error: true
        run: |
          echo "Trying CHEAP pod..."
          RESPONSE=$(curl -sS -X POST "https://api.runpod.io/graphql" \
            -H "Authorization: Bearer ${{ secrets.RUNPOD_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{\"query\":\"mutation { podResume(input: { podId: \\\"${{ secrets.RUNPOD_CHEAP_POD_ID }}\\\" }) { id desiredStatus } }\"}")

          echo "$RESPONSE"

          if echo "$RESPONSE" | grep -q '"errors"'; then
            echo "CHEAP pod unavailable."
            exit 1
          fi

          echo "CHEAP pod start request succeeded."

      - name: Try mid pod if cheapest and cheap failed
        if: steps.cheapest.outcome == 'failure' && steps.cheap.outcome == 'failure'
        run: |
          echo "Trying MID pod..."
          RESPONSE=$(curl -sS -X POST "https://api.runpod.io/graphql" \
            -H "Authorization: Bearer ${{ secrets.RUNPOD_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{\"query\":\"mutation { podResume(input: { podId: \\\"${{ secrets.RUNPOD_MID_POD_ID }}\\\" }) { id desiredStatus } }\"}")

          echo "$RESPONSE"

          if echo "$RESPONSE" | grep -q '"errors"'; then
            echo "CHEAPEST, CHEAP, and MID pods are unavailable."
            exit 1
          fi

          echo "MID pod start request succeeded."

      - name: Wait for services
        run: |
          echo "Waiting 90 seconds for services to boot..."
          sleep 90

      - name: Check status
        run: |
          curl -sS -X POST "https://api.runpod.io/graphql" \
            -H "Authorization: Bearer ${{ secrets.RUNPOD_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{\"query\":\"query { myself { pods { id name desiredStatus runtime { uptimeInSeconds ports { privatePort publicPort type } } } } }\"}"
